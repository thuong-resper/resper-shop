import { makeStyles } from '@material-ui/core/styles';
export const useStyles = makeStyles((theme) => ({
  background: {
    backgroundColor: '#fff',
    borderRadius: 4,
    marginBottom: '1.5rem',
  },

  formControl: {
    minWidth: 120,
    width: '100%',
  },
}));
